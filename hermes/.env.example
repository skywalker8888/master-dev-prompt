# Notion integration token (Settings -> My integrations -> Hermes Dispatcher)
NOTION_TOKEN=secret_your_token_here

# Hermes database ID (the 32-character ID in the database URL)
DATABASE_ID=your_database_id_here

# Tier 3 only
WEBHOOK_PORT=5000
MAX_PARALLEL_TASKS=3
# Shared secret the caller must send as the X-Webhook-Secret header on
# POST /webhook. Leave unset only for local testing.
WEBHOOK_SECRET=change_me
