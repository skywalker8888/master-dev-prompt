# Core — always required
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0        # used by AllegroAdapter remote mode

# --- GPU / model dependencies (install on GPU hosts only) ----------------
# CogVideoX
# torch>=2.3.0
# diffusers>=0.29.0
# transformers>=4.41.0
# accelerate>=0.30.0
# imageio[ffmpeg]>=2.34.0

# Stable Video Diffusion (same stack as CogVideoX, add Pillow)
# Pillow>=10.3.0

# Allegro (local mode uses same diffusers stack)
# (no extra packages beyond CogVideoX set)
